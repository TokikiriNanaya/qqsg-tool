version: "3.8"

services:
  mysql:
    image: mysql:5.7
    container_name: qqsg-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: qqsg
      MYSQL_USER: qqsg
      MYSQL_PASSWORD: qqsg
      TZ: Asia/Shanghai
    ports:
      - "9941:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-u", "qqsg", "-pqqsg"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 40s

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: qqsg-backend
    restart: always
    environment:
      APP_NAME: "QQ三国工具"
      APP_VERSION: "1.0.0"
      DEBUG: "false"
      DATABASE_URL: "mysql+pymysql://qqsg:qqsg@mysql:3306/qqsg?charset=utf8mb4"
      SECRET_KEY: "0d000721"
      ALGORITHM: "HS256"
      ACCESS_TOKEN_EXPIRE_MINUTES: "1440"
      REFRESH_TOKEN_EXPIRE_DAYS: "7"
      ALLOWED_ORIGINS: '["*"]'
      TZ: Asia/Shanghai
    volumes:
      - ./sql/data:/app/sql/data   # 数据文件，每次启动自动同步
      - ./sql/init:/app/sql/init   # 建表文件，首次初始化时执行
    ports:
      - "9940:9940"
    depends_on:
      mysql:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: qqsg-frontend
    restart: always
    ports:
      - "9939:80"
    depends_on:
      - backend

volumes:
  mysql_data:
