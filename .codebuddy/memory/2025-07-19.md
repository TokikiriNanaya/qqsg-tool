# 2025-07-19 工作记录

## 前后端合并到单容器（用户撤销）
- 用户自行撤销了前后端合并的改动，恢复为三个独立服务

## 数据库生命周期统一由 Python 管理
- **移除** `docker-compose.yml` 中 `./sql/init:/docker-entrypoint-initdb.d` 挂载，MySQL 容器不再负责初始化
- **新增** `bootstrap_database()` 函数在 `data_sync.py`，统一管理数据库初始化/迁移/数据同步
- 启动流程：
  - 检测 users 表是否存在 → 判断是否首次初始化
  - 首次：执行 `sql/init/` 建表 → `alembic stamp head` → 导入 `sql/data/` 数据
  - 非首次：Alembic 自动迁移 → 数据同步
- `00_init.sql` 放在 `sql/init/` 目录，建表语句和数据文件分开管理
- 更新 `docker-entrypoint.sh`，调用 `bootstrap_database()` 替代原来的分步流程

## 全面审查修复 (2025-07-19)
- 修复 `backend/Dockerfile`：去掉无效的前端多阶段构建（`../frontend` COPY 会导致构建失败），恢复为纯 Python 后端构建
- 修复 `00_init.sql` 位置：移回 `sql/init/` 目录（之前误放在 `sql/data/`）
- 修复 `data_sync.py`：去掉未使用的 `engine` 导入；去掉 `sync_official_data()` 中跳过 `00_init.sql` 的死代码
- 修复 `main.py`：去掉重复的 `import os`

## 添加 README.md
- 创建 `README.md`，合并 `STARTUP.md` 内容
- 包含：项目概述、技术栈、Docker 部署、本地开发、数据管理、调试技巧
- 删除 `STARTUP.md`

## Docker 构建国内网络优化
- `backend/Dockerfile`：`apt-get update` 前替换为清华 apt 镜像源
- `README.md`：新增"配置 Docker 镜像加速"前置步骤（registry-mirrors），解决 `node:18-alpine`/`nginx:alpine` 拉取卡住问题
- `.env.example`：改为本地开发开箱即用的默认值（`root:root@localhost`）
